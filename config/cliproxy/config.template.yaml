# CLIProxy config template — generated by vein --setup
# Sentinel comments mark auto-generated sections; manual edits outside sentinels are preserved.

# --- vein-managed: routing ---
routing:
  strategy: round-robin
  session-affinity: true
# --- end vein-managed: routing ---

# --- vein-managed: defaults ---
claude-header-defaults:
  stabilize-device-profile: true
request-retry: 3
max-retry-credentials: 2
# --- end vein-managed: defaults ---

# --- vein-managed: accounts ---
# Account blocks injected by vein --accounts add
# --- end vein-managed: accounts ---
